use yew::prelude::*;

use crate::main_home;
use crate::header::ja::Header;
use crate::home::{MainHomeBase, MenuProps};
use crate::name::en::*;
use crate::url::Lang::En as lang;
use crate::url::*;

main_home!();