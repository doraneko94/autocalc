use yew::prelude::*;

use crate::main_home;
use crate::header::ja::Header;
use crate::home::{MainHomeBase, MenuProps};
use crate::name::ja::*;
use crate::url::Lang::Ja as lang;
use crate::url::*;

main_home!();